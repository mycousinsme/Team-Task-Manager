[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && npm install && npm run build && cd ../backend && npm install"

[deploy]
startCommand = "NODE_ENV=production node backend/src/index.js"
healthcheckPath = "/api/health"
